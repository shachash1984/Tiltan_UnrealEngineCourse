#include <iostream>
#include "Player.h"
#include "Vertex.h"
#include "Point.h"
#include "Mesh.h"

using namespace std;

int main()
{  
    //Point* point = new Point();
    //cout << point->ToString();

    //Color* color = new Color(0.4f, 0.4f, 0.4f, 0.4f);
    //cout << color->ToString() + "\n";
    //delete color;
    //color = new Color(5, 0.2f, -4, 1.2f);
    //cout << color->ToString();

    //Vertex* vertex = new Vertex();
    //cout << vertex->GetPosition().ToString();

    //Mesh* mesh = new Mesh();
    //cout << mesh->ToString();

    //Mesh* mesh = new Mesh();
    //cout << mesh->GetVertexWorldPosition(4).ToString();

    //Player* player = new Player("Gal", new Mesh(), new Mesh());
    //cout << player->ToString();

    //Mesh* _bodyOfGal = new Mesh();
    //Mesh* _weaponOfGal = new Mesh();
    //Player* player = new Player("Gal", _bodyOfGal, _weaponOfGal);
    //cout << player->ToString();
}