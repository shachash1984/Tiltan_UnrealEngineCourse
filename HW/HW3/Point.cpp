#include <iostream>
#include "Point.h"

Point::Point() // Need to initialize the variables and not assign them -5
{
	x = 0;
	y = 0;
	z = 0;
}

//Point::Point() : x(0), y(0), z(0) // This is the correct way to initialize the variables
//{
//}
